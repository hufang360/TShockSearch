# 更新日志

## 20260901（v1.4.1）

- 移除独立的 `/shimmer`（`/shi`）指令与 `hf.shimmer` 权限：嬗变信息已完全并入 `/r` 合成查询显示，不再需要单独的查询指令；配置项 `aliasShimmer` 一并移除（旧配置多余字段自动忽略）。
- 「嬗变而来」显示样式与嬗变一致：`来源物品 @ 微光 -> 结果`，不再单独列一个物品列表。
- 代码整理：`Shimmer` 改为静态工具类，仅保留 `BuildLines(id)` 供 `/r` 合并展示调用。

## 20260830（v1.4.0）

- 新增 物品嬗变（微光）信息，并入 `/r` 合成查询显示（不再有独立的 `/shimmer` 指令）：
  - 嬗变：该物品扔进微光后变成什么（含月总后解锁提示）；
  - 分解：扔进微光后还原成哪些合成材料（带数量图标，含击败骷髅王/石巨人后解锁提示）；
  - 嬗变而来：哪些物品扔进微光会变成它；
  - 尊重原版等价物品映射（水桶算水、蜂蜜瓶算蜂蜜等）；硬币特判（微光中转化为财运，不产出物品）；
  - 显示与合成类似：`物品 @ [i:5362]微光 -> 结果`；与原版一致，物品有嬗变就不会触发分解（嬗变优先），不会同时出现两者；
  - 涉及物品同合成信息一样汇总到底部「备注」行；无配方但有嬗变信息时直接显示嬗变信息；

- 修复 1.4.5 下合成制作站不显示的问题：`Recipe.requiredTile` 在 1.4.5 由数组改为单个 int，旧兼容分支走空；现在兼容 int/数组两种格式，祭坛、铁砧等制作站条件恢复正常，同时补齐 `needTorchGodsFavor`（火把神祝福）、`needMechdusa`（机械美杜莎）两个新合成条件。

- 优化 合成信息底部的翻页信息中，物品标签在高亮内不显示的问题。
- 优化 分页行数：新增 `maxLinesPerPage` 配置项（默认 8，原来固定 4），`/r`、`/search`、`/shi` 分页统一生效。
- 优化 “无配方”提示：`无[i:6173]名称(6173)的配方…` 改为 `[i:6173]名称(6173)，无合成配方，也不是合成材料！`（图标+名称+id，取消数字高亮）。
- 优化 合成条件 显示：（蜂蜜/水/岩浆/雪原/灵雾/火把神祝福/机械美杜莎）文字左侧增加物品图标。
- 优化 查询性能：新增名称索引缓存，首次查询懒构建“名称→id”索引（wiki 修正名 + 当前语言名 + 中文名），精确/模糊匹配走内存字典，不再每次搜索全表读库并遍历全部物品；`/search import` 导入 wiki 后自动失效重建。数据库层重构：连接改用连接池 + `using` 自动释放，杜绝泄漏；初始化批量写入改参数化，不再拼接 SQL。
